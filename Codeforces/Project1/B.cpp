#include <iostream>
#include <string>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);
    int n, k, a, b;
    string name = "ºñ¿ÍÀÌ";
    cin >> n >> k;
    while (n--)
    {
        cin >> a >> b;
    }
    cout << name << '\n';
}