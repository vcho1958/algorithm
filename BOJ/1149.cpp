#include<cstdio>
#include<cstdlib>
enum a{R,G,B};
int main(){
  int RGB[3][1000], N, sum = 0, (*third)[1000] =NULL, (*second)[1000] = NULL, (*first)[10000] = NULL;
  scanf("%d", &N);
  for(int i = 0 ; i < N; i++){
    scanf("%d %d %d", &RGB[R][i], &RGB[G][i], &RGB[B][i]);
  }
  for(int i = 1; i < N-1;i++){
    if(RGB)
  }
}